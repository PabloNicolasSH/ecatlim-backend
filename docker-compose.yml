version: '3.8'
services:
  db:
    image: mysql:8.0
    restart: no
    environment:
      MYSQL_DATABASE: 'ecatlim'
      MYSQL_USER: 'admin'
      MYSQL_PASSWORD: 'password'
      MYSQL_ROOT_PASSWORD: 'password'
    ports:
      - '3306:3306'
    expose:
      - '3306'